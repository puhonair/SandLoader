node_modules/
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local mods that are not part of the project
mods/*
!mods/example-hello/
!mods/example-missions/

# Fetched by install.js, not part of the source tree.
vendor/
Fluxloader mods/

# Subagent-driven-development scratch: ledger, briefs, review packages.
.superpowers/

# A scratch workspace that lives in this checkout and is not part of SandLoader.
# Untracked by hand until now, which only held because nothing ever ran
# `git add -A`. Naming it here is what actually keeps it out.
mod-creator/

# Development probes and reverse-engineering scratch files
tools/_*
out.txt
selftest-output.txt
temp_selftest.txt

